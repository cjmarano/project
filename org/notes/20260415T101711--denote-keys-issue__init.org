#+title:      Denote keys issue
#+date:       [2026-04-15 Wed 10:17]
#+filetags:   :init:
#+identifier: 20260415T101711

S-b for new denote note stopped working. Problem was with:

(use-package denote
  :ensure t
  :hook (dired-mode . denote-dired-mode)
  :bind
  ;; (("C-c n r" . denote-rename-file)
  ;;  ("C-c n l" . denote-link)
  ;;  ("C-c n c" . denote-link-after-creating)
  ;;  ("C-c n b" . denote-backlinks)
  ;;   )
  :config
(setq denote-directory (expand-file-name "~/project/org/notes/"))
(setq denotes-known-keywords '("emacs" "init" "general" "shell"))
(setq completion-styles '(substring basic))
(setq denote-file-type nil))
(keymap-global-set "s-b" 'denote)

-- keymap-global-set "s-b" had to be moved out of the use-package section.
(it was above setq denote file type inside the parens opened by (user-package denote

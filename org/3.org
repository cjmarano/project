#+PROPERTY: folded
;;; +June.+
* Start times down on New and Big to > 2 on New after fixing flymake-ruff.
** Start times up die to global-flycheck-mode - 16% of total startup time.
** ISSUE After flymake-ruff added start time went from < 1 to > 2 secs on New.
CLOSED: [2026-06-08 Mon 11:01]
* Misc
** COMPLETED  C-c r is now bound to recentf-save-list because list wasn't reliably updating on New.
CLOSED: [2026-03-27 Fri 08:35]
** COMPLETED Look at journal or org-roam for ability to step back in time to see changes.
CLOSED: [2026-04-13 Mon 09:41]
*** dailies go in ~/project/org/roam/daily
C-c n d n to capture a new note.
C-c n i to insert link to another note.
C-c n d d to see Today's notes.
C-c n f to see list of all notes.
*** C-c c then j for  journal (and not C-c j) C-c a does open agenda.
*** Org journal entries all go into the same file --> journal.org
while org roam dailies go into sepearte files (even if created on same day) with leading date.
*** how are org roam dailies and denote notes different?
*** Majpr feature of Roam is ability to x-linkg notes and a db so search is easier.
** use-package infers :defer t so package does not load immediately.
*** with straight can use :straight t
** _Note:_ did NOT change require statements that looked like part of fuctions (not sepearte on own line).
*** COMPLETED Tried to install an info item.
CLOSED: [2026-04-29 Wed 12:58]
*** Secret was to add /opt/homebrew/share/info/ to info-additional-directory-list.
install-info --info-dir=/opt/homebrew/share/info ~/Software/muse.info
*** Tried adding /opt/homebrew/share/info to info-default-directory-list but it did not transfer to info-directory-list.
*** doing:   !%/opt/homebrew/opt/texinfo/bin!/info!%   gave texinfo display, including muse.info so...
*** muse.info probably not showing up in emacs because it was a texinfo file, not an info file.
*** comaprison of texinfo Top Directory and emacs info Top Directory proves they are pulling from two different places.
 the texinfo stuff seems to be the standard GNU stuff that should be in info. The stuff in emacs appears to be odd-ducks.
** re-installed homebrew emacs, deleted alias in .zshrc and linked brew emacs to emacs in .zshrc
CLOSED: [2026-05-05 Tue 09:33]
** So... moved ~/.emacs.d/elpa to ~/back_elpa/ then installed Straight Bootstrap.
*** removed all mention of package from init.el
*** lot of work to replace require with use-package commented out a lot of stuff to get a working init.
** More work stil to be done. Left eglot intact but killed all lsp-mode stuff and a lot of Rust stuff.
*** quicklisp/slime-helper loaded.
*** both big and new have rainbow-delimiters in straight/repos but init.el complains.
** COMPLETED Both New and Big working w/ straight version of init.el
*** DONE New & Big moved to straight. Don't change init.el.org needed for Plucky & Ducky.
CLOSED: [2026-05-15 Fri 11:02]
** .zshrc path checked on New and Big and a copy sent to ~/project/
** Comparing Elpa and Repos to see what's in the old init that's not in the new.
** Do a diff on init.el for new and big to see what's different?
** COMPLETED fixed org-empasis on Big;  _red_ +green+ *BOLD* /italic/ =verbatim= ~code~
CLOSED: [2026-06-01 Mon 13:54]
** COMPLETED Slime was _not_ installed on New or Big.
CLOSED: [2026-06-01 Mon 13:06]
** elisp file - init.el - does connect to Company backends. Lisp files don't.
** Changed use-package slime to straight-use-package 'slime  and it installed.
*** slime now works on both Macs.
** I am getting full everything on init.el (which is elisp) but not on lisp files.
** COMPLETED _Lisp_ on New - can't complete capf not connected. But lisp on Big can.
CLOSED: [2026-06-01 Mon 11:16]
is using company-dabbev-code company-gtags company-etags company-keywords.
** _Lisp_: Company: backend company-capf error "Not connected." with args (prefix)
** C-h v then company-backends shows no backends for lisp or elisp on New.
** capf for lisp IS working on Big. see capf_lisp.txt for more.
** COMPLETED Global-company-mode was _not_ working. Fixed on New and Big.
CLOSED: [2026-06-01 Mon 13:06]
** commented out some lines and moved global-company-mode below use-package:
(use-package company
  :straight t)
;;   :bind (:map company-active-map
;;               ("<tab>" . company-complete-selection))
;;           (:map python-mode-map
;;               ("<tab>" . company-indent-or-complete-common))
;;   :custom
;; (company-minimum-prefix-length 1)
;; (company-idle-delay 0.5))

(add-hook 'after-init-hook 'global-company-mode)

** You can force company to find dabbreve by using company-begin-backend
and then choosing the dabbrev backend from the list that appears in the minibuffer.
** company-diag from Big:
Emacs 30.2 (aarch64-apple-darwin23.2.0) of 2025-12-05 on builder-m1-14.lan
   2 │ Company 1.0.2
   3 │
   4 │ company-backends: (company-bbdb company-semantic company-cmake company-capf
   5 │               company-clang company-files
   6 │               (company-dabbrev-code company-gtags company-etags
   7 │                                     company-keywords)
   8 │               company-oddmuse company-dabbrev)
   9 │
  10 │ Used backend: company-dabbrev
  11 │
  12 │ Major mode: lisp-mode

  versus New:

  company-call-backend-raw: Company: backend company-capf error "Not connected." with args (prefix)
  
  company-backends variable looks the same on both machines. Init.el same on both machines.

  company-mode repos are same on both machines.
  -----------------------------------------
** if slime is running everything works on New, Company & Company-box.
** make SLIME connect to your lisp when you open a lisp file: add this to your .emacs:
(add-hook 'slime-mode-hook
(lambda ()
(unless (slime-connected-p)
(save-excursion (slime)))))
** COMPLETED Can NOT get org-tempo set up.
CLOSED: [2026-06-04 Thu 14:51]
** may be set up, will test. Blew up init.el on New, had to reset a LOT.
** Got denote-journal set up:
S-i to make new entry.
S-r to open entry or make new one.
C-c n t to add like to an ex
** Don't use journal to organize thoughts.
*** Even if opened on emacs in org-mode it mixes up time/date info.
*** I want last in at bottom or top and sorted by create date.
*** I do not like the sort order that journal entries have.
*** if you open the journal.org file in emacs it does sort by date.
*** org-roam /might/ be better but I don't want to bother.
*** Maybe Denote. To open denote files can C-x, C-f then use orderless to search for
names fron the vertico list in the minibuffer.
*** Denote notes will bet harder to search as more are created with similar names. Prot is extending
Denote all the time so this may NOT be a big deal.
** This version of emacs says NOT built with native compile. 
** lsp, lspUI not needed.
** COMPLETED [#B] had to comment out (use-package flymake-ruff) Look into this.
CLOSED: [2026-06-08 Mon 07:41]
Problem was ' not needed before package name.
** Ran straight-pull-all and broke init.el
Issue was with Slime. Tried to edit instead of stash when told it was dirty.
This broke init.el = santiy color theme gone from ~/.emacs.d/themes/ so I guess straight pull
deletes items first then pulls them from git.
** Failed: try M-x straight-normalize-package (or straight-normalize-all)
** DONE Added (setq doom-modeline-project-name t) to init.el
CLOSED: [2026-06-08 Mon 13:56]

** NEXT Cleaned up notebook.org - need to spend time using emacs to edit.
** COMPLETED Python sytntax highlighting, marginalie and Company working on New and Big but,
CLOSED: [2026-06-08 Mon 13:42]
** DONE Python setup on New. _Maybe?_
** DONE eglot failing. Still can't find pylsp _maybe fixed?_.
CLOSED: [2026-06-08 Mon 07:58]
** ISSUE +Python+-mode/eglot broken on New, but not on Big!
** Internet says it's because emacs is using project.el not project.elc
*** Rolling back init.el a week (17May) did not fix Python Problem on New.
** +Python+ on New - capf: HOMEBREW_NO_PATH_SHADOW_CHECK=1 in .zshrc to force emacs
to use homebrew pylsp instead of shims/pylsp. Big was using /opt/homebrew//bin/pylsp and working.
Which command in terminal still shows /Users/charles.marano/.pyenv/shims/pylsp which is odd.
** Python: when starting eglot have to tell emacs where pylsp is even though...
it's on the $PATH /Users/charles.marano/.pyenv/shims/pylsp
*** running M-x eglot prompts for localtion of pylsp...
giving it /Users/charles.marano/.pyenv/shims/pylsp starts eglot and it seems to work.
** json rpc message is from eglot - it can't find might be from closing progrs.
** building Python language grammar w/ M-x trees-t-install-language-grammar
did re-enable syntax higlighting and getting some things in the minibuffer.
** Finish setting up Python w/tree-sitter and eglot, no lsp. Yes to UV.
** DONE Created ~/project/Prot/straight_prot.el as backup in case init.el blows up.
CLOSED: [2026-06-09 Tue 11:17]
this is the straight version of ~/project/Prot/init.el and works.
** ISSUE Make org mode files look nicer.
CLOSED: [2026-06-10 Wed 13:28]
** DONE Could not find theme better than sanity-tomorrow-eighties.
CLOSED: [2026-06-10 Wed 13:20]
* Rust, Rustic, wait.
* DONE Completion-Preview mode mode is now built in to emacs.
CLOSED: [2026-06-12 Fri 12:09]
<TAB> to accept M-i to complete up to longest common prefix.
M-x completion-preview-next-candidate cycles fwd
 M-x completion-preview-prev-candidate cycles back
* WAITING LISP. Using (ql:quickload "cl-project") then
(cl-project:make-project #P"./quicklisp/local-projects/my-projects/")
got a skeleton for the project and a main.lisp in the scr subdir.
looks like this:

drwxr-xr-x@    - charles.marano 25 Feb 12:55   src
drwxr-xr-x@    - charles.marano 25 Feb 12:55   tests
.rw-r--r--@   68 charles.marano 25 Feb 12:55   .gitignore
.rw-r--r--@  559 charles.marano 25 Feb 12:55   my-project.asd
.rw-r--r--@   40 charles.marano 25 Feb 12:55   README.markdown
.rw-r--r--@   41 charles.marano 25 Feb 12:55   README.org

main.lisp looks like this:
                                        
(uiop:define-package my-project
   2 │   (:use #:cl))
   3 │ (in-package #:my-project)
   4 │
   5 │ ;; blah blah blah.
* TODO Lisp mode working on Big but have to start Slime first on New. Why?
** WAITING Lisp connects on Big, not on New _unless_ slime run 1st.
* 
